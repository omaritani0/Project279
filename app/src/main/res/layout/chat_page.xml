<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/background_photo_guidlines">

    <!-- Title for Pinned Chats -->
    <TextView
        android:id="@+id/pinnedChatsTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pinned Chats"
        android:textSize="18sp"
        android:layout_margin="16dp" />

    <!-- RecyclerView for Pinned Chats -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/pinnedChatsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:layout_weight="0" />

    <!-- Title for Recent Chats -->
    <TextView
        android:id="@+id/recentChatsTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Recent Chats"
        android:textSize="18sp"
        android:layout_margin="16dp" />

    <!-- RecyclerView for Recent Chats -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recentChatsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_margin="8dp"
        android:layout_weight="1" />

    <!-- Floating Action Button for New Chat -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/newChatButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_search"
        android:layout_gravity="end|bottom"
        android:layout_margin="16dp" />

</LinearLayout>
