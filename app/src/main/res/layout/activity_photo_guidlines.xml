<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_photo_guidlines"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="16dp"> <!-- Center everything vertically and horizontally -->

    <!-- Logo ImageView -->
    <ImageView
        android:id="@+id/logoImageView"
        android:layout_width="wrap_content"
        android:layout_height="153dp"
        android:layout_marginBottom="16dp"
        android:layout_marginTop="-64dp"
        android:src="@drawable/img_1" />

    <!-- Centered Container for Buttons -->
    <LinearLayout
        android:layout_width="251dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"> <!-- Vertically and horizontally center the content inside this LinearLayout -->

        <!-- Setup a Meeting Button -->
        <Button
            android:id="@+id/setup_meeting_button"
            android:layout_width="237dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:backgroundTint="@android:color/holo_orange_dark"
            android:text="Setup a Meeting" />

        <!-- Go to Chat Button -->
        <Button
            android:id="@+id/chat_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:backgroundTint="@android:color/holo_orange_dark"
            android:text="Go to Chat" />

        <ImageButton
            android:id="@+id/search_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginBottom="8dp"
            android:contentDescription="Hamburger Menu"
            android:src="@drawable/menu"
            android:backgroundTint="@android:color/holo_orange_dark"
            android:scaleType="centerInside"
            android:background="?attr/selectableItemBackground" />
    </LinearLayout>

    <!-- Profile Info Section (You can keep it as is, it will appear below the buttons) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center_horizontal">

        <!-- Text within a relative position to the background image -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="-100dp"
            android:gravity="center_horizontal"
            android:orientation="vertical"> <!-- Adjust this to position text over image -->

            <TextView
                android:id="@+id/name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:fontFamily="sans-serif-medium"
                android:textColor="#ffffff"
                android:textSize="24sp" />
        </LinearLayout>
    </LinearLayout>

</LinearLayout>
